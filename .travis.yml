language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bw-labs' ]; then cd .. && eval "mv $currentfolder generator-bw-labs" && cd generator-bw-labs; fi

