doctype html
html(lang="en")
  head
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title= services.config.app.name
<% if(options['use-bootstrap']) {%>    link(href="//cdn.jsdelivr.net/bootstrap/3.1/css/bootstrap.min.css", rel="stylesheet") <% } %>
<% if(options['use-vendor-css']) {%>    link(href="/css/vendor.css", rel="stylesheet")<% } %>
    block styles
  body
    block content
<% if(options['use-bootstrap']) {%>    script(src="//cdn.jsdelivr.net/g/jquery@2.1,bootstrap@3.1") <% } %>
<% if(options['use-vendor-js']) {%>    script(src="/js/vendor.js")<% } %>
    block scripts